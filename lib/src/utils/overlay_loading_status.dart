/// Enum representing the loading status of an overlay.
enum OverlayLoadingStatus {
  /// The overlay is visible and actively loading.
  visible,

  /// The overlay is hidden and not loading.
  hidden,
}
